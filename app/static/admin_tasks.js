const state={page:1,totalPages:0,sortBy:'create_time',sortOrder:'desc'};
const $=id=>document.getElementById(id);
const esc=value=>String(value??'').replace(/[&<>'"]/g,ch=>({'&':'&amp;','<':'&lt;','>':'&gt;',"'":'&#39;','"':'&quot;'}[ch]));
const labels={pending:'待执行',running:'执行中',completed:'已完成',failed:'失败'};
function params(){const p=new URLSearchParams({page:state.page,page_size:20,sort_by:state.sortBy,sort_order:state.sortOrder});[['project_id','project'],['review_version','version'],['task_type','taskType'],['state','state']].forEach(([key,id])=>{if($(id).value)p.set(key,$(id).value)});if($('dateFrom').value)p.set('date_from',new Date($('dateFrom').value).toISOString());if($('dateTo').value)p.set('date_to',new Date($('dateTo').value).toISOString());return p}
async function load(){const response=await fetch('/api/admin/tasks?'+params());if(!response.ok)throw new Error('加载失败');const data=await response.json();state.totalPages=data.total_pages;$('total').textContent=`${data.total} 个任务`;$('pageInfo').textContent=`${data.page} / ${Math.max(1,data.total_pages)}`;$('prev').disabled=data.page<=1;$('next').disabled=data.page>=data.total_pages;$('rows').innerHTML=data.items.length?data.items.map(item=>`<tr><td>${esc(item.project_id)}</td><td><a href="${esc(item.report_path)}">${esc(item.review_version)}</a></td><td>Polyspace 确认</td><td><span class="status ${esc(item.status)}">${labels[item.status]||item.status}</span></td><td class="red-count">${item.red_issue_num}</td><td>${item.issue_num}</td><td>${new Date(item.create_time).toLocaleString()}</td></tr>`).join(''):'<tr><td colspan="7">没有匹配的任务</td></tr>'}
$('search').onclick=()=>{state.page=1;load()};$('refresh').onclick=load;$('reset').onclick=()=>{document.querySelectorAll('.filters input,.filters select').forEach(el=>el.value='');state.page=1;load()};$('prev').onclick=()=>{if(state.page>1){state.page--;load()}};$('next').onclick=()=>{if(state.page<state.totalPages){state.page++;load()}};document.querySelectorAll('[data-sort]').forEach(button=>button.onclick=()=>{const field=button.dataset.sort;state.sortOrder=state.sortBy===field&&state.sortOrder==='asc'?'desc':'asc';state.sortBy=field;state.page=1;load()});
load().catch(error=>$('rows').innerHTML=`<tr><td colspan="7">${esc(error.message)}</td></tr>`);

